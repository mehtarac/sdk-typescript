name: Secure Integration test

on:
  pull_request:
    branches: main

jobs:
  authorization-check:
    permissions: read-all
    runs-on: ubuntu-latest
    outputs:
      approval-env: ${{ steps.collab-check.outputs.result }}
    steps:
      - name: Collaborator Check
        uses: actions/github-script@v8
        id: collab-check
        with:
          result-encoding: string
          script: |
            try {
              const permissionResponse = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: context.payload.pull_request.user.login,
              });
              const permission = permissionResponse.data.permission;
              const hasWriteAccess = ['write', 'admin'].includes(permission);
              if (!hasWriteAccess) {
                console.log(`User ${context.payload.pull_request.user.login} does not have write access to the repository (permission: ${permission})`);
                return "manual-approval"
              } else {
                console.log(`Verifed ${context.payload.pull_request.user.login} has write access. Auto Approving PR Checks.`)
                return "auto-approve"
              }
            } catch (error) {
              console.log(`${context.payload.pull_request.user.login} does not have write access. Requiring Manual Approval to run PR Checks.`)
              return "manual-approval"
            }
  check-access-and-checkout:
    runs-on: ubuntu-latest
    needs: authorization-check
    environment: ${{ needs.authorization-check.outputs.approval-env }}
    permissions:
      id-token: write
      pull-requests: read
      contents: read

    steps:
      - name: Configure Credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
         aws-region: us-east-1
         mask-aws-account-id: false

      - name: Get Current Role Info
        run: |
          echo "Current IAM Principal:"
          aws sts get-caller-identity
          echo "---"

      # - name: Assume Cross-Account Role and Fetch Secrets
      #   id: fetch-secrets
      #   run: |
      #     echo "Assuming cross-account role for Secrets Manager access..."
          
      #     # Assume the cross-account role
      #     CREDS=$(aws sts assume-role \
      #       --role-arn "arn:aws:iam::430853190378:role/ApiKeySecretReader" \
      #       --role-session-name "GitHubActionsSecretsAccess" \
      #       --duration-seconds 3600)
          
      #     # Extract credentials
      #     export AWS_ACCESS_KEY_ID=$(echo $CREDS | jq -r '.Credentials.AccessKeyId')
      #     export AWS_SECRET_ACCESS_KEY=$(echo $CREDS | jq -r '.Credentials.SecretAccessKey')
      #     export AWS_SESSION_TOKEN=$(echo $CREDS | jq -r '.Credentials.SessionToken')
          
      #     echo "Successfully assumed cross-account role"
          
      #     # Fetch the secret
      #     echo "Fetching API keys from Secrets Manager..."
      #     SECRET_VALUE=$(aws secretsmanager get-secret-value \
      #       --secret-id "model-provider-api-key" \
      #       --region us-east-1 \
      #       --query 'SecretString' \
      #       --output text)
          
      #     # Parse and export API keys
      #     echo "OPENAI_API_KEY=$(echo $SECRET_VALUE | jq -r '.openai // empty')" >> $GITHUB_ENV
          
      #     echo "Successfully loaded API keys"

      - name: Checkout head commit
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }} # Pull the commit from the forked repo
          persist-credentials: false  # Don't persist credentials for subsequent actions

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Build the package
        run: npm run build

      - name: Run integration tests
        env:
          AWS_REGION: us-east-1
          STRANDS_TEST_API_KEYS_SECRET_NAME: ${{ secrets.STRANDS_TEST_API_KEYS_SECRET_NAME }}
        run: npm run test:integ
